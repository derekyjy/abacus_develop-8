# ABACUS 近邻搜索模块动态负载均衡效果分析

## 1. 测试方案

在 `module_neighlist::BinManager::build_atom_neighbors` 中对比两种 OpenMP 调度策略：

| 调度策略 | 机制 | 负载特性 |
|----------|------|----------|
| `schedule(static)` | 等量连续块分配给各线程，分配后固定 | 无负载均衡，线程间可能出现严重等待 |
| `schedule(guided, 16)` | 线程动态领取递减大小的任务块（最小16） | 动态负载均衡：先完成工作的线程可以继续领取新任务 |

测试用异构原子分布：**10% 原子在稠密簇（0.05 box） + 90% 原子稀疏分布（全box）**，原子按稠密→稀疏顺序排列。

## 2. 测试结果

测试环境：16 线程 OpenMP（MSVC /O2），search_radius = 0.35

### 2.1 单线程 → 多线程加速比（guided调度优化前 vs 优化后）

| 原子数 | 串行(ms) | 16线程(ms) | 加速比 |
|--------|----------|------------|--------|
| 5,000 | 62.9 | 20.6 | 3.0x |
| 10,000 | 244.1 | 56.6 | 4.3x |
| 20,000 | 941.6 | 170.0 | 5.5x |
| 50,000 | 5,724.9 | 818.7 | 7.0x |
| 100,000 | 22,154.4 | 3,554.4 | 6.2x |

> 加速比随原子数增加而提升（3.0x → 7.0x），说明问题规模越大，并行红利越大。但始终远低于理想线性的 16x。

### 2.2 静态调度 vs 引导调度（同级并行下的动态均衡效果）

| 场景 | 原子数 | static(ms) | guided16(ms) | guided/static |
|------|--------|------------|-------------|---------------|
| 倾斜分布 | 10,000 | 66.4 | 68.5 | 0.97x |
| 倾斜分布 | 20,000 | 268.3 | 278.7 | 0.96x |
| 倾斜分布 | 50,000 | 1,226.5 | 1,264.9 | 0.97x |
| 均匀分布 | 10,000 | 259.3 | 253.4 | 1.02x |
| 均匀分布 | 50,000 | 6,187.0 | 6,199.5 | 1.00x |

> **static 和 guided 在线程层面几乎无差异**（波动在 ±4% 以内），动态调度并未带来预期中的负载均衡改善。

## 3. 效果不佳的根因分析

### 3.1 `#pragma omp critical` 是主要串行瓶颈

```cpp
// bin_manager.cpp build_atom_neighbors 内部
int* ptr = nullptr;
#pragma omp critical          // ← 所有线程在此串行化
{
    ptr = neighbor_list.allocator.allocate(n);
}
```

`PageAllocator` 为全局共享资源，所有线程通过临界区串行分配内存。这导致：

- 每个线程在处理完一个原子的近邻搜索后，必须排队等待进入临界区
- 临界区串行时间虽短（~几十纳秒），但每个原子都要进入一次
- 当 16 个线程同时竞争临界区时，15 个线程处于等待状态
- **Amdahl定律**：即使99%的代码可并行，1%的串行部分也会将16核加速比上限限制在 ~13.9x。临界区开销占比越高，加速上限越低

### 3.2 旧模块无此瓶颈（对比参考）

```cpp
// sltk_grid.cpp Construct_Adjacent (旧模块)
// 每个原子直接写入预分配的 all_adj_info[type][natom]
// 各线程写入不同槽位，天然无锁，无需临界区
all_adj_info[fatom1.type][fatom1.natom].push_back(fatom2);
```

旧模块的 `all_adj_info[type][natom]` 按 (type, 原子索引) 预分配，每个线程处理不同的原子，写入不同的槽位，**天然无锁**。这是旧模块的 guided 调度能起效的前提——没有临界区把线程绑在一起。新模块的 `PageAllocator` 打破了这一设计。

### 3.3 搜索计算 vs 内存分配的开销比失衡

| 操作 | 耗时占比（估） | 是否可并行 |
|------|---------------|-----------|
| 遍历27个相邻bin + 距离计算 | ~70% | 是（各原子独立） |
| `PageAllocator::allocate()` (临界区) | ~20% | **否**（串行化） |
| 内存复制 neighbor → page | ~10% | 是 |

临界区占比 ~20% 意味着 Amdahl 上限 ≈ `1 / 0.2 = 5x`，这与实测的 5-7x 加速比基本吻合。guided 调度无法消除临界区等待，因为所有线程都要经过同一道锁。

### 3.4 负载倾斜度不足以凸显调度差异

在测试的 "10%稠密+90%稀疏" 场景下，稠密区原子占 50000 × 10% = 5000 个。16 线程 static 调度各分 ~3125 个原子，稠密原子只跨约 1.6 个线程——大部分线程处理的是同质稀疏原子，负载本身已经接近均衡。要显现 guided 的优势需要更极端的分布（如 2%/98% 或原子有序排列在多个梯度区间）。

